@page
@model HotelReservationSystem.Pages.IndexModel

@{
    ViewData["Title"] = "Home";
}

<h2>Available Rooms</h2>

@if (Model.Rooms != null && Model.Rooms.Any())
{
    <div class="room-list">
        @foreach (var room in Model.Rooms)
        {
            <div class="room-item">
                <!-- Display the room number -->
                <h3>@room.RoomNumber</h3>
                
                <!-- Display the room image -->
                <img src="@room.ImageURL" alt="Image of @room.RoomNumber" style="width:200px;height:150px;object-fit:cover;" />

                <!-- Display the room description -->
                <p>@room.Description</p>
                
                <!-- Display the room price -->
                <p>Price: $@room.Price</p>
                
                <!-- Optionally, display availability status -->
                <p>Status: @(room.IsAvailable ? "Available" : "Not Available")</p>
            </div>
        }
    </div>
}
else
{
    <p>No rooms available at the moment.</p>
}

@if (!User.Identity.IsAuthenticated)
{
    <p>Please <a asp-page="/Account/Login">login</a> to view more details.</p>
}
